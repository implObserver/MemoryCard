import styles from '../styles/Description.module.css'

export const Description = () => {
    return (
        <div className={styles.description}>
            <span>Click on any card but don't click more than once on the same card!</span>
        </div>
    )
}